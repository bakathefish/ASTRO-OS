---
# AstroOS: plymouth's own spinner theme with the AstroOS watermark (installed by
# astroos-branding's 85 hook), on every GPU. Replaces the CachyOS theme packages
# that upstream selects here.
plymouth_theme: spinner
plymouth_theme_amdgpu: spinner
