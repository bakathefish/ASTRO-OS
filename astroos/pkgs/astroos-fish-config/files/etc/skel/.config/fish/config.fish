# AstroOS fish configuration, the copy every new user gets from /etc/skel.
# Derived from the CachyOS package of the same purpose (MIT).
# This file is yours to edit. pacman never rewrites it once it has been
# copied into your home directory, so put your own settings below.
source /usr/share/astroos-fish-config/astroos-config.fish

# overwrite greeting
# potentially disabling fastfetch
# (this file is read after conf.d, so a definition here wins over the one in
# astroos-branding's conf.d/astroos-greeting.fish and over the one above)
#function fish_greeting
#    # smth smth
#end
